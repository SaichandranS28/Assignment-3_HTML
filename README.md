1. What is the Shortcut Emmet Used to Create boilerplate of HTML ?
   We can create basic html boilerplate using ! and html:5

2. What is DOCTYPE in Html ?
   It is to inform this is the HTML document and the version being used to the web browser.
   
3. What is Void Element and Example for Void Elements ?
   Void elements are especially dont have any closing tags, Eg. <br>, <hr> <img>, <input>, <link>
   
4. What is Different between Element and Attributes ?
   Element defines the document structure and the content, whereas Attributes can be used to style/modify the elements
   
5. What is Html Entries and Why is it need in HTML ?
   It is used to display characters that are reserved in HTML (such as < and >) or characters that are not readily available on a standard keyboard (like symbols and foreign letters). 

6. What are meta tag and why is it used ?
   It lies in the head section of the html page, A pieces of information you use to tell the search engines and those viewing your site more about your page and the information it contains.
   Meta tags include: Title tags: the title of your page, which should be unique for every page you publish. Meta description: a description of the content on the page.
   
7. what is the best way to add images in Website ?
   We can add images from both in our local storage / we can save it in a CDN websites like imagekit.io
   By Using CDN, we can render the image more easily and fastly rather than using local storage. It speeds the performance of the website.
